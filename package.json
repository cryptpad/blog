{
  "dependencies": {
    "@11ty/eleventy": "^1.0.1",
    "@11ty/eleventy-plugin-rss": "^1.2.0",
    "@11ty/eleventy-plugin-syntaxhighlight": "^4.2.0",
    "less": "^4.1.3",
    "markdown-it-attrs": "^4.1.6"
  },
  "scripts": {
    "generate": "npx @11ty/eleventy --input=. --output=docs",
    "build": "rm -rf public && npx @11ty/eleventy --input=. --output=docs",
    "dev": "npx @11ty/eleventy --serve --output=docs"
  }
}
