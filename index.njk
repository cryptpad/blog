---
layout: layouts/main
eleventyExcludeFromCollections: true
---


{% for item in collections.all | reverse %}
    <article>
        <a href="{{ item.url }}" ><h2>{{ item.data.title }}</h2></a><br>
        {% for tag in item.data.tags %}
            <a href="/tags/{{ tag | slugify }}">{{ tag }}</a> 
        {% endfor %}
    </article>
{% endfor %}
